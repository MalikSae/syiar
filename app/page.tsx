/**
 * app/page.tsx — Halaman root domain.
 * Placeholder verifikasi middleware: tampil ketika akses via localhost:3000.
 * Akan diganti halaman sesungguhnya di sprint berikutnya.
 */
export default function RootPage() {
  return (
    <main style={{ fontFamily: 'monospace', padding: '2rem' }}>
      <p>ROOT DOMAIN</p>
    </main>
  )
}
